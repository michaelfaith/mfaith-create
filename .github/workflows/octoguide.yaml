name: OctoGuide

on:
  discussion:
    types:
      - created
      - edited
  discussion_comment:
    types:
      - created
      - deleted
      - edited
  issue_comment:
    types:
      - created
      - deleted
      - edited
  issues:
    types:
      - edited
      - opened
  pull_request_review_comment:
    types:
      - created
      - deleted
      - edited
  pull_request_target:
    types:
      - edited
      - opened

jobs:
  octoguide:
    if: ${{ !endsWith(github.actor, '[bot]') && !contains(github.event.pull_request.labels.*.name, 'autorelease') }}
    runs-on: ubuntu-latest
    permissions:
      discussions: write
      issues: write
      pull-requests: write
    steps:
      - uses: JoshuaKGoldberg/octoguide@990f9bb3d914189128a7fd7d01490a2d9037e1f1 # 0.23.2
        with:
          config: strict
          github-token: ${{ secrets.GITHUB_TOKEN }}
          rules: |-
            {
              "pr-branch-non-default": false
            }
